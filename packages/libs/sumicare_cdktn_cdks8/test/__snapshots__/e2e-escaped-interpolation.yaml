provider:
  kubernetes:
    - alias: cdktn-cdk8s-cdk8s-provider
resource:
  kubernetes_manifest:
    cdk8s-provider_cdk8s-provider-v1-ConfigMap-app-config-default_3F1D0CD5:
      manifest:
        apiVersion: v1
        data:
          BUCKET_NAME: bucket-$${env}-name
        kind: ConfigMap
        metadata:
          name: app-config
      provider: kubernetes.cdktn-cdk8s-cdk8s-provider
terraform:
  required_providers:
    kubernetes:
      source: hashicorp/kubernetes
      version: 3.2.1
