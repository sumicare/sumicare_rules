{
	"name": "@sumicare/organization",
	"version": "0.1.0",
	"license": "MIT",
	"private": false,
	"type": "module",
	"scripts": {
		"clean": "run-p clean:*",
		"clean:cache": "rm -rf node_modules/.cache",
		"clean:dist": "rm -rf dist",
		"clean:turbo": "rm -rf .turbo"
	},
	"devDependencies": {
		"@types/node": "^26.1.1",
		"npm-run-all": "^4.1.5",
		"typescript": "^7.0.2"
	}
}
